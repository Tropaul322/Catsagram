name: "Lint project"

on:
  push

jobs:
  lint-project:
    # environment: test
    runs-on: ubuntu-latest
    env: 
      PORT: ${{ vars.PORT }}
      TYPEORM_HOST: ${{ vars.TYPEORM_HOST }}
      TYPEORM_CONNECTION: ${{ vars.TYPEORM_CONNECTION }}
      TYPEORM_USERNAME: ${{ vars.TYPEORM_USERNAME }}
      TYPEORM_PASSWORD: ${{ vars.TYPEORM_PASSWORD }}
      TYPEORM_DATABASE: ${{ vars.TYPEORM_DATABASE }}
      TYPEORM_PORT: ${{ vars.TYPEORM_PORT }}
      JWT_SECRET: ${{ vars.JWT_SECRET }}
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - run: echo "🔎 The name of your branch is${{ env.TYPEORM_DATABASE }} ${{ env.JWT_SECRET }} and your repository is ${{ github.repository }}."
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with: 
          node-version: 14
      # - name: Lint react-ts
      #   run: |
      #     npm i -g nx
      #     npm i
      #     nx test api