name: "Lint project"

on:
  push

jobs:
  lint-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with: 
          node-version: 14
      - run: cd ./react-ts
      - run: npm ci
      - run: npm run lint
